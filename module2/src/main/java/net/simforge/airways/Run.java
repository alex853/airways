package net.simforge.airways;

import net.simforge.airways.engine.Engine;
import net.simforge.airways.engine.EngineBuilder;
import net.simforge.airways.engine.entities.TaskEntity;
import net.simforge.airways.persistence.AirwaysApp;

import java.util.Queue;

public class Run {
    public static void main(String[] args) {
/*        new AirwaysApp.StartupAction().run();

        Engine engine = EngineBuilder.create()
                .withSessionFactory(AirwaysApp.getSessionFactory())
                .withTimeMachine(new DefaultTimeMachine())
                .build();

        final Queue<TaskEntity> taskQueue = engine.getTaskQueue();

        new Thread(() -> {

        }).start();
*/
    }
}
